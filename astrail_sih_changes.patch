diff --git a/frontend/app.js b/frontend/app.js
index 950e8cb..f4f57a6 100644
--- a/frontend/app.js
+++ b/frontend/app.js
@@ -233,14 +233,14 @@ function renderOrbits(tracks, highlightNorads) {
     const isHighlighted = highlightNorads.has(track.norad_id);
     const pts = track.points.map((p) => new THREE.Vector3(p[0], p[2], p[1]));
     const geo = new THREE.BufferGeometry().setFromPoints(pts);
-    const color = isHighlighted ? 0xff5d72 : 0x2d5f8a;
-    const mat = new THREE.LineBasicMaterial({ color, transparent: true, opacity: isHighlighted ? 0.9 : 0.35 });
+    const color = isHighlighted ? 0xff5d72 : 0x4be3a6;
+    const mat = new THREE.LineBasicMaterial({ color, transparent: true, opacity: isHighlighted ? 0.9 : 0.4 });
     orbitGroup.add(new THREE.Line(geo, mat));
 
     // Marker sits at the object's *current* position (first sampled point,
     // t=now); the line shows where it's headed over the look-ahead window.
     const current = pts[0];
-    const markerColor = isHighlighted ? 0xff5d72 : 0x5fd8ff;
+    const markerColor = isHighlighted ? 0xff5d72 : 0x4be3a6;
     const markerGeo = new THREE.SphereGeometry(isHighlighted ? 140 : 90, 10, 10);
     const markerMat = new THREE.MeshBasicMaterial({ color: markerColor });
     const marker = new THREE.Mesh(markerGeo, markerMat);
@@ -477,9 +477,12 @@ function currentParams() {
 }
 
 /* ================= source status (sidebar) ================= */
+let backendHasConnected = false;
+
 function setSourceChip(source) {
   const chip = document.getElementById("sideDot").parentElement;
   const label = document.getElementById("sideSourceLabel");
+  const sub = document.getElementById("sideSourceSub");
   chip.className = "side-status";
   const map = {
     live: ["source-live", "LIVE · CELESTRAK"],
@@ -490,6 +493,26 @@ function setSourceChip(source) {
   const [cls, text] = map[source] || ["", "UNKNOWN"];
   if (cls) chip.classList.add(cls);
   label.textContent = text;
+  if (sub) sub.style.display = "none";
+}
+
+// First-ever backend response of the session gets a brief "SYSTEM ONLINE"
+// flash before settling into the normal source chip (LIVE / DEMO / CACHED),
+// so a judge watching the wake-up from Render's free tier sees a clear
+// "it's alive" moment rather than jumping straight to a source label.
+function markBackendOnline(source) {
+  if (backendHasConnected) {
+    setSourceChip(source);
+    return;
+  }
+  backendHasConnected = true;
+  const chip = document.getElementById("sideDot").parentElement;
+  const label = document.getElementById("sideSourceLabel");
+  const sub = document.getElementById("sideSourceSub");
+  chip.className = "side-status source-online";
+  label.textContent = "SYSTEM ONLINE";
+  if (sub) sub.style.display = "none";
+  setTimeout(() => setSourceChip(source), 1100);
 }
 
 /* ================= OVERVIEW page rendering ================= */
@@ -791,7 +814,7 @@ function renderInsight(kessler) {
   el.innerHTML = `Across <b>${state.events.length}</b> detected conjunction${state.events.length > 1 ? "s" : ""},
     risk is currently driven more by <b>${driver}</b> than the other factor
     (avg proximity score ${avgProx.toFixed(1)} vs. avg velocity score ${avgVel.toFixed(1)}).
-    The Kessler Index sits at <b>${kessler.index.toFixed(1)}</b> (${kessler.label.toLowerCase()}),
+    The ASTRAIL Risk Index sits at <b>${kessler.index.toFixed(1)}</b> (${kessler.label.toLowerCase()}),
     with the soonest closest approach between <b>${soonest.object_a.name}</b> and
     <b>${soonest.object_b.name}</b> in about <b>${soonest.hours_to_tca.toFixed(1)}h</b>.`
     .replace(/\s+/g, " ");
@@ -814,7 +837,7 @@ async function runScan() {
     const orbitsRes = await fetch(API.orbits(groups, demo, Math.min(hours, 6)));
     const orbitsData = await orbitsRes.json();
     lastTracks = orbitsData.tracks;
-    setSourceChip(orbitsData.source);
+    markBackendOnline(orbitsData.source);
     log(`Ingested ${orbitsData.tracks.length} tracked objects (${orbitsData.source}).`);
     document.getElementById("objectCountVal").textContent = orbitsData.tracks.length;
     state.objectCount = orbitsData.tracks.length;
@@ -822,13 +845,19 @@ async function runScan() {
     renderOrbits(lastTracks, new Set());
   } catch (err) {
     log(`Orbit ingestion failed: ${err}`, "err");
+    if (!backendHasConnected) {
+      const label = document.getElementById("sideSourceLabel");
+      const sub = document.getElementById("sideSourceSub");
+      label.textContent = "STILL WAKING UP…";
+      if (sub) sub.style.display = "block";
+    }
   }
 
   try {
     const res = await fetch(API.conjunctions(groups, demo, hours, threshold));
     const data = await res.json();
     state.events = data.events;
-    setSourceChip(data.source);
+    markBackendOnline(data.source);
 
     log(`Conjunction scan complete: ${data.events.length} events within ${threshold}km.`,
         data.events.length ? "warn" : "");
@@ -860,6 +889,24 @@ async function runScan() {
   }
 }
 
+/* ================= quick demo CTA ================= */
+document.getElementById("quickDemoBtn").addEventListener("click", () => {
+  document.getElementById("demoToggle").checked = true;
+  hoursSlider.value = 48;
+  document.getElementById("hoursVal").textContent = "48h";
+  threshSlider.value = 25;
+  document.getElementById("threshVal").textContent = "25 km";
+  // Demo Mode restricts itself to the offline synthetic catalog regardless
+  // of which object sets are selected, but pick sensible ones anyway so the
+  // Object Sets panel makes sense if a judge glances at it afterward.
+  Array.from(document.getElementById("groupSelect").options).forEach((o) => {
+    o.selected = ["stations", "cosmos-1408-debris", "iridium-33-debris", "cosmos-2251-debris"].includes(o.value);
+  });
+  goToPage("live");
+  log("Quick Demo: synthetic catalog · 48h window · 25km threshold.");
+  runScan();
+});
+
 /* ================= boot ================= */
 window.addEventListener("load", () => {
   init3D();
diff --git a/frontend/index.html b/frontend/index.html
index 81c5206..bf7f131 100644
--- a/frontend/index.html
+++ b/frontend/index.html
@@ -47,8 +47,9 @@
     <div class="side-footer">
       <div class="side-status">
         <span class="dot" id="sideDot"></span>
-        <span id="sideSourceLabel">CONNECTING…</span>
+        <span id="sideSourceLabel">INITIALIZING ORBITAL DATA…</span>
       </div>
+      <div class="side-status-sub" id="sideSourceSub">Free server may require up to 60s on first launch.</div>
       <div class="side-clock"><span id="utcClock">--:--:--</span> UTC</div>
     </div>
   </nav>
@@ -59,12 +60,12 @@
     <header class="statusbar">
       <div class="statusbar-title" id="pageTitle">Overview</div>
       <div class="statusbar-right">
-        <div class="mini-stat"><span id="objectCountVal">0</span><label>objects</label></div>
-        <div class="mini-stat"><span id="alertCountVal">0</span><label>alerts</label></div>
+        <div class="mini-stat"><span id="objectCountVal">—</span><label>objects</label></div>
+        <div class="mini-stat"><span id="alertCountVal">—</span><label>alerts</label></div>
         <div class="mini-kessler" id="miniKessler">
           <span class="mini-kessler-dot"></span>
-          <span id="miniKesslerVal">0.0</span>
-          <label>kessler</label>
+          <span id="miniKesslerVal">—</span>
+          <label>risk idx</label>
         </div>
       </div>
     </header>
@@ -78,9 +79,13 @@
           <p>ASTRAIL pulls public TLE data, propagates every tracked object with SGP4, and flags conjunctions
              before they happen with a single number that tells you how bad the neighborhood looks right now.</p>
           <div class="hero-actions">
-            <button class="btn-primary" data-goto="live">▸ Open Live Tracking</button>
+            <button class="btn-primary" id="quickDemoBtn">▸ RUN QUICK DEMO</button>
+            <button class="btn-ghost" data-goto="live">Open Live Tracking</button>
             <button class="btn-ghost" data-goto="methodology">How it works</button>
           </div>
+          <p class="hero-hint">Quick Demo loads the offline synthetic catalog, a 48h window, and a sensible
+             alert threshold, then runs a scan for you. Fine-tune everything under Advanced Scan Parameters
+             in Live Tracking.</p>
         </div>
         <div class="hero-visual">
           <svg viewBox="0 0 260 260" class="pulse-svg">
@@ -110,7 +115,7 @@
           <span class="kpi-sub" id="kpiAlertsSub">within threshold</span>
         </div>
         <div class="kpi-card">
-          <label>Kessler Index</label>
+          <label>ASTRAIL Risk Index</label>
           <span class="kpi-value" id="kpiKessler">—</span>
           <span class="kpi-sub" id="kpiKesslerLabel">nominal</span>
         </div>
@@ -141,7 +146,7 @@
 
         <aside class="panel panel-left">
           <div class="card">
-            <h2>Mission Parameters</h2>
+            <h2>Advanced Scan Parameters</h2>
             <label class="field">
               <span>Object Sets</span>
               <select id="groupSelect" multiple size="4">
@@ -181,16 +186,17 @@
               <svg viewBox="0 0 200 120" id="kesslerSvg">
                 <path d="M10,110 A90,90 0 0,1 190,110" class="gauge-track"/>
                 <path d="M10,110 A90,90 0 0,1 190,110" class="gauge-fill" id="gaugeFill"/>
-                <text x="100" y="90" class="gauge-value" id="kesslerValue">0.0</text>
-                <text x="100" y="108" class="gauge-label" id="kesslerLabel">NOMINAL</text>
+                <text x="100" y="90" class="gauge-value" id="kesslerValue">—</text>
+                <text x="100" y="108" class="gauge-label" id="kesslerLabel">AWAITING SCAN</text>
               </svg>
             </div>
             <div class="kessler-meta">
-              <h2>Kessler Index</h2>
+              <h2>ASTRAIL Kessler Risk Index</h2>
+              <p class="kessler-subtitle">Experimental catalog-wide risk indicator · 0–100</p>
               <p>Catalog-wide collision risk, aggregated across all detected conjunctions in the current window.</p>
               <div class="kessler-counts">
-                <span class="pill critical" id="criticalCount">0 CRITICAL</span>
-                <span class="pill high" id="highCount">0 HIGH</span>
+                <span class="pill critical" id="criticalCount">— CRITICAL</span>
+                <span class="pill high" id="highCount">— HIGH</span>
               </div>
             </div>
           </div>
@@ -241,9 +247,9 @@
           <canvas id="compChart" class="chart-canvas" height="220"></canvas>
         </div>
         <div class="card">
-          <h2>Kessler Index — Session History</h2>
+          <h2>ASTRAIL Risk Index — Session History</h2>
           <canvas id="trendChart" class="chart-canvas" height="220"></canvas>
-          <p class="chart-note">Tracks the Kessler Index across scans run during this session. Run a new scan to add a point.</p>
+          <p class="chart-note">Tracks the ASTRAIL Risk Index across scans run during this session. Run a new scan to add a point.</p>
         </div>
         <div class="card wide">
           <h2>Top Objects by Conjunction Involvement</h2>
@@ -287,9 +293,12 @@
           <li><b>Data confidence (10%)</b> — older TLE epochs carry more positional uncertainty.</li>
         </ul>
 
-        <h2>05 · Kessler Index</h2>
-        <p>A single 0–100 dial summarizing catalog-wide risk: 70% from the average of the five highest-risk events
-           in the window, 30% from how many risky conjunctions are piling up relative to catalog size.</p>
+        <h2>05 · ASTRAIL Risk Index</h2>
+        <p>A single 0–100 dial summarizing catalog-wide risk. This is an ASTRAIL-designed aggregate indicator,
+           not an established or peer-reviewed standard — it is inspired by the cascading-collision risk
+           associated with the Kessler effect, but the formula is our own: 70% from the average of the five
+           highest-risk events in the window, 30% from how many risky conjunctions are piling up relative to
+           catalog size.</p>
 
         <h2>Limitations</h2>
         <p>This is a triage and demonstration tool, not a certified conjunction-assessment system. It does not model
diff --git a/frontend/style.css b/frontend/style.css
index 935efc4..9b237db 100644
--- a/frontend/style.css
+++ b/frontend/style.css
@@ -131,6 +131,8 @@ a:hover{text-decoration:underline;}
 .side-status.source-live .dot{background:var(--nominal);color:var(--nominal);}
 .side-status.source-demo .dot{background:var(--amber);color:var(--amber);}
 .side-status.source-cache .dot{background:var(--accent);color:var(--accent);}
+.side-status.source-online .dot{background:var(--nominal);color:var(--nominal);}
+.side-status-sub{font-size:9.5px;line-height:1.4;color:var(--text-faint);letter-spacing:0.2px;margin-top:-4px;}
 .side-clock{font-family:var(--font-mono);font-size:11px;color:var(--accent);letter-spacing:0.5px;opacity:.9;}
 
 /* ---------------- main area ---------------- */
@@ -197,7 +199,8 @@ a:hover{text-decoration:underline;}
 .hero-copy h1{font-family:var(--font-display);font-weight:600;font-size:31px;line-height:1.32;
   margin:0 0 18px;color:var(--text);letter-spacing:0.1px;}
 .hero-copy p{font-size:15px;line-height:1.75;color:var(--text-dim);max-width:520px;margin:0 0 26px;}
-.hero-actions{display:flex;gap:12px;}
+.hero-actions{display:flex;gap:12px;flex-wrap:wrap;}
+.hero-hint{font-size:12px;line-height:1.6;color:var(--text-faint);max-width:520px;margin:14px 0 0;}
 
 .btn-primary,.btn-ghost{
   padding:12px 22px;border-radius:10px;font-family:var(--font-body);font-weight:600;
@@ -304,6 +307,8 @@ input[type=checkbox]{accent-color:var(--amber);width:16px;height:16px;}
 .gauge-value{font-family:var(--font-display);font-size:27px;fill:var(--text);text-anchor:middle;font-weight:600;}
 .gauge-label{font-family:var(--font-mono);font-size:10px;fill:var(--text-faint);text-anchor:middle;letter-spacing:1.8px;}
 .kessler-meta p{font-size:12px;color:var(--text-dim);margin:0 0 10px;line-height:1.6;}
+.kessler-subtitle{font-family:var(--font-mono);font-size:10px !important;letter-spacing:0.6px;
+  color:var(--text-faint) !important;text-transform:uppercase;margin:-4px 0 10px !important;}
 .kessler-counts{display:flex;gap:8px;}
 .pill{font-size:10px;letter-spacing:0.6px;padding:4px 10px;border-radius:20px;font-family:var(--font-mono);
   backdrop-filter:blur(6px);}
